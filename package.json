{
  "name": "quizproject-web",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone frontend for Quiz Project",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  }
}
